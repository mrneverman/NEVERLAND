apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  profile: default
  hub: gcr.io/istio-testing
  tag: latest
  components:
    pilot:
      k8s:
        nodeSelector:
          kubernetes.io/hostname: monitortown
        tolerations:
        - key: "purpose"
          operator: "Equal"
          value: "monitoring"
          effect: "NoSchedule"

